<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
<%= link_to Doctor.find(@appointment.doctor_id).name, doctor_path(Doctor.find(@appointment.doctor_id)) %>
<%= link_to Patient.find(@appointment.patient_id).name, patient_path(Patient.find(@appointment.patient_id)) %>